# 📑 08.5.5 銜接日誌：從偏好對齊（Alignment）到大文本吞吐（Data Throughput）的範式轉換

> 本篇作為模組五（DPO 偏好對齊）與模組六（CPT 文本打包與流式吞吐）之間的技術橋樑。我們將徹底理清從「教導模型明辨是非」到「對海量私有語料進行吞吐與預訓練」時的架構轉變，幫助初學者精確掌握兩者在底層資料結構、記憶體定址以及算力優化上的本質區別。

## 🧭 1. 任務特徵躍遷：從賽局邏輯回歸極致高吞吐

在模組五中，我們的核心任務是「對齊」**（Alignment），利用 Policy 模型與 Reference 模型的雙軌賽局，拉開滿意回答（Chosen）與討厭回答（Rejected）之間的隱式獎勵差。 進入模組六後，戰場重新移回了**「增量預訓練」**（Continued Pre-Training, CPT）。此時我們不再處理偏好對錯，而是要讓模型在最短時間內**吞噬數以 GB/TB 計的全新領域私有語料，目標是最大化硬體算力利用率，並守住記憶體防禦封鎖線。

### 資源、資料結構與控制調度對比矩陣

|**維度**|**模組五：DPO Alignment**|**模組六：Continued Pre-Training (CPT)**|
|---|---|---|
|**技術本質**|**行為塑造（Behavior Shaping）**：教導模型明辨是非，符合人類偏好。|**知識注入（Knowledge Injection）**：吞噬海量文本，學習領域基底知識。|
|**資料基本型態**|**三元組資料（Preference Triad）**：<br><br>  <br><br>`{"prompt", "chosen", "rejected"}`|**單向長文本流（Raw Text Stream）**：<br><br>  <br><br>不分對錯的純文字語料（如技術文件、財報）。|
|**記憶體佈局**|**雙軌模型並行**：Policy 與 Reference 同時定址，記憶體開銷翻倍。|**單軌模型 + 高效緩衝區**：僅需單個模型，但需維護一個 `TokenPackingEngine` 狀態機。|
|**計算核心與算子**|多維 `gather` 算子提取 Token-Level 對數機率矩陣。|自迴歸 **Shift 算子**（預測下一個 Token）與交叉熵損失（CrossEntropyLoss）。|
|**硬體優化痛點**|記憶體容量（Capacity）：雙軌前向傳播容易引爆 OOM。|硬體頻寬與算力浪費：傳統右側補零（Padding）浪費大量 Tensor Core 算力。|

## 🔄 2. 核心架構目的：我們究竟在解決什麼物理限制？

對於剛接觸大模型工程的開發者而言，模組六的代碼看起來像是繁瑣的「文字切分與拼接遊戲」，但從底層維運與系統架構來看，手寫這套組件是為了解決兩個極致現實的硬體瓶頸：

### 核心目的 A：消除 Padding 算力廢熱（對齊 Tensor Core 效率）

- **現狀破口**：傳統 DataLoader 會將短文本用 `[PAD]`（補零）填滿至 `max_seq_len`（例如 2048）。當語料充斥大量短文本時，矩陣中高達 50% 以上可能都是無意義的零。這會導致顯示晶片平白耗費大量電力去計算「0 × 權重」。
    
- **實作目的**：手寫 `TokenPackingEngine` 就像是文字版的「俄羅斯方塊」，它把無數短文本用 `[EOS]` 拼接成長龍，並精確切成 2048 長度的 Block。**矩陣裡 100% 全是實體文字，讓算力利用率（MFU）直接拉到頂峰**。
    

### 核心目的 B：破除 50GB 語料的 OOM 崩潰（常數級記憶體守護）

- **現狀破口**：普通數據加載器（如 `Dataset`）需要一次性把文件讀進記憶體並做 Tokenize。當你塞入數十 GB 的技術文件時，宿主機的 RAM 會在一微秒內被抽乾，觸發系統死機。
    
- **實作目的**：整合 PyTorch `IterableDataset` 實作 **Streaming（流式加載）**。硬碟就像是一條單向傳送帶，程式每次只讀取一行、Tokenize 一行、塞入狀態機一行的 Token，內存佔用恆定為 $O(1)$ 常數空間，不論語料檔案是 10GB 還是 1TB，本地端都能流暢跑通。
    

## 🔀 3. 資料與控制流轉生命週期 (Pipeline Blueprint)

以下為模組六從硬碟文本源，一向滑行、無損打包並注入模型訓練的系統全鏈路控制流圖：

```
[ 硬碟：cpt_corpus.txt ]  <-- 數 GB 原始私有語料大檔案 (常駐硬碟)
       │
       ▼ 【單向游標滑行讀取：逐行 Streaming 載入】 
       │   由 StreamingPackingDataset (IterableDataset) 驅動，內存開銷 O(1)
       │
[ 記憶體暫存：單行純文字 ]
       │
       ▼ 【Tokenizer 編碼轉換】
       │   轉換為離散的整型 Token ID 陣列 list[int]
       │
[ 狀態機緩衝區：TokenPackingEngine.residual_buffer ]
       │   1. 自動在文字尾端追加對齊隔離符 [EOS]
       │   2. 循環檢查：只要累積長度 ≥ max_seq_len (2048)，立刻切分吐出
       │
[ 完美定長數據塊：{"input_ids": [2, 2048]} ]
       │
       ▼ 【強型態守門員： nn.Embedding 投影層】
       │   將離散 Token ID 映射為連續的稠密特徵矩陣 [2, 2048, 128]
       │
[ 活躍特徵張量：hidden_states ]
       │
       ▼ 【手寫自迴歸訓練迴路：CPTTrainingPipeline】
       │   1. 前向傳播吐出 Logits [2, 2048, Vocab_Size]
       │   2. 執行 Shift 算子（錯開一位進行因果遮罩下一個字預測）
       │   3. 計算 CrossEntropyLoss 梯度下降更新 LoRA 參數
       │
[ 權重進化：累積領域智慧 ]
```

## 🛡️ 5. 模組六進入前之架構防禦焦點 (Pre-flight Checklist)

在開始編寫或閱讀模組六程式碼前，請務必將以下技術防禦錨點深植大腦，這也是防止系統產生隱蔽 Bug 的核心關鍵：

1. **Streaming 與 Batch_Size 的物理限制**：
    
    因為流式加載（`IterableDataset`）沒有固定的資料長度，它是一邊讀取一邊發射。因此在配合 `DataLoader` 時，絕對**不能**使用常規的 `shuffle=True` 亂序算子，否則會破壞流式指針並引爆記憶體緩衝區。
    
2. **Causal LM 的 Shift 位移對齊合約**：
    
    增量預訓練採用自迴歸機制（預測下一個字）。在計算 Loss 時，必須將模型輸出（Logits）的前 $T-1$ 個位置，與標籤（Labels）的後 $T-1$ 個位置進行對齊（即 `logits[..., :-1, :]` 與 `labels[..., 1:]`）。如果忘記做這個位移，模型將會變成在「作弊比對當前的字」，導致訓練 Loss 異常低下，但模型實際上什麼都沒學到。
    
3. **清倉閘門（Flush Mechanism）的邊界防禦**：
    
    當整份大文件全部讀完時，狀態機內部通常還會殘留一些不足 2048 長度的文字碎片。架構上必須設計 `flush_remaining()` 函數，在文本流乾涸時強制將其進行一次性右側補零倒出，確保領域語料的最後一段知識不會丟失。
    

本篇日誌到此錨定完工，您現在已經具備了清晰的技術全景視角，隨時可以流暢推進至 **[08.6 模組六技術實作文件]**，親手落實這套極致高吞吐的數據管道！