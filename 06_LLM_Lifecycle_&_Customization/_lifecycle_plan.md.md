# 🏛️ 84小時進修實作模組 與 Obsidian 筆記目錄映射對照表
> 專門用來錨定 7 個開發模組的「執行時間線」與 2 大類筆記的「空間架構線」，確保整體推進過程中不迷路、不走歪。

| 7 個實作模組（執行時間線） <br><nobreak><div style="width:180px;"></div></nobreak> | 映射到的 Obsidian 筆記位置 <br><nobreak><div style="width:220px;"></div></nobreak>                                                              | 模組具體實作任務與代碼落實細節（防走歪檢查清單）                                                                                                                                                                                                                                                                                                                          |
| :--------------------------------------------------------------------- | :-------------------------------------------------------------------------------------------------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **模組一：動態組態與型態安全架構**<br>(Time Budget: 6h)                               | `_mlops_index.md`<br>└─ `07.1_dynamic_config_&_safety.md`                                                                               | 1. 實作 Hydra 階層式 YAML 配置，抽離 $r, \alpha, \text{lr}$ 與環境變數。<br>2. 撰寫 Pydantic v2 BaseSettings 守門員邏輯，進行超參數邊界校驗。<br>3. 實作 Runtime 硬體偵測器，自動配置 `device="mps"` 並動態調配並行 Thread 數量。                                                                                                                                                                         |
| **模組二：多核心加速 Data Curation 管道**<br>(Time Budget: 16h)                   | `_llm_customization_index.md`<br>└─ `06.1_data_curation_&_packing.md`<br><br>`_mlops_index.md`<br>└─ `07.4_automated_e2e_validation.md` | 1. 實作 Python `multiprocessing` 核心調度，並行化大文本清洗與標準化。<br>2. 手寫 **MinHash + LSH（局部敏感雜湊）** 演算法，落實海量文本百萬級別去重。<br>3. 遵循 TDD 規範，於 `tests/` 針對 Data Cleaner 撰寫 `pytest` 單元測試（含空字串、異常編碼、極端長文本等邊界條件）。                                                                                                                                                         |
| **模組三：非同步自動化基準測試引擎**<br>(Time Budget: 12h)                             | `_mlops_index.md`<br>└─ `07.3_async_evaluation_&_metrics.md`                                                                            | 1. 使用 Python `asyncio` 實作非同步高併發外部 LLM-as-a-Judge 呼叫管線。<br>2. 設計嚴格的評分提示詞（Rubric 1-5分盲測標準），控制裁判模型的輸出格式。<br>3. 封裝 `Evaluate` 套件，實作微調前後 `ROUGE-1/2/L`、`BLEU`、`Exact Match` 指標計算與自動化 Markdown 差異報告導出。                                                                                                                                                  |
| **模組四：手寫 PyTorch + MPS 訓練迴路**<br>(Time Budget: 24h)                    | `_llm_customization_index.md`<br>└─ `06.2_training_dynamics_&_mps.md`                                                                   | 1. 捨棄 High-level Trainer 封裝，親手實作 `loss.backward()`、`optimizer.step()` 與梯度裁剪（Gradient Clipping）的 Custom Loop。<br>2. 實作 **Gradient Accumulation（梯度累積）** 邏輯，在單機受限環境下模擬大 Batch Size 訓練效果。<br>3. 整合 `torch.profiler.profile` 與 `torch.mps.current_allocated_memory()`，實時打印 M4 Pro Max 統一記憶體分配與 Trace 瓶頸。<br>4. 實作 LoRA 權重儲存、載入與 Base Model 的 Merge 邏輯。 |
| **模組五：DPO 數據管道與損失函數實作**<br>(Time Budget: 10h)                          | `06.3_alignment_&_dpo_loss.md`                                                                                                          | 1. 實作 DPO 偏好數據 `{"prompt", "chosen", "rejected"}` 的結構、對齊度與長度嚴格校驗管線。<br>2. 純手寫 **$L_{DPO}$ 損失函數**，利用 PyTorch Tensor Ops 實作矩陣對數機率（Log Likelihood）運算。<br>3. 於代碼 Docstring 內使用 $\LaTeX$ 註解核心公式推導，釐清 Chosen/Rejected 的機率差優化邏輯。                                                                                                                         |
| **模組六：Continued Pre-Training 大文本吞吐**<br>(Time Budget: 6h)              | `_llm_customization_index.md`<br>└─ `06.1_data_curation_&_packing.md`                                                                   | 1. 手寫工業級 **Packing（文本打包）演算法**，將多篇短文用 `endoftext` 連接符拼接，並精確切分成固定長度（如 2048 tokens）的 Block，消除 Padding 算力浪費。<br>2. 整合 PyTorch `IterableDataset` 實作 Streaming 流式加載，確保處理數 GB 私有文本時內存不擠爆。                                                                                                                                                                |
| **模組七：工業級 MLOps 觀測與自動化驗證**<br>(Time Budget: 10h)                       | `_mlops_index.md`<br>└─ `07.2_tracking_&_observability.md`<br><br>`_mlops_index.md`<br>└─ `07.4_automated_e2e_validation.md`            | 1. 將手寫訓練迴路原生對接 **WandB / MLflow**，非同步即時推播 Loss、Learning Rate 與硬體佔用率。<br>2. 撰寫 **End-to-End 全生命週期整合測試腳本 (`test_e2e_lifecycle.py`)**，餵入極小 Mock 數據一鍵回歸「組態->清洗->CPT->LoRA->DPO->評估」完整管線，確保系統 **CI/CD-Ready**。                                                                                                                                         |