當人類（Ace）進行最終檢視，確認代碼質量完美、測試覆蓋率無誤，在 GitLab 介面上點擊 **Merge**。

- **自動化收尾**：
    
    - GitLab 觸發 Pipeline 將程式碼併入主線。
        
    - Webhook 觸發結案腳本，調用 API 將對應的 Issue 關閉。
        
    - Gemini 讀取本次 MR 的變更，自動在 GitLab Release 頁面或專案 `CHANGELOG.md` 中，生成符合語意化版本（Semantic Versioning）的變更日誌。